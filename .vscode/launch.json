{
  "configurations": [
    {
      "name": "Attach to Chrome",
      "port": 9222,
      "request": "attach",
      "type": "chrome",
      "webRoot": "${workspaceFolder}"
    },
    {
      "type": "pwa-chrome",
      "name": "localhost:4200",
      "request": "launch",
      "url": "localhost:4200"
    }
  ]
}
